const initialState = {
    isReduxWorking: true
}


const sample = (state=initialState, action) =>{
    switch (action) {
        case 'value':
            
            break;
    
        default:
            return state
    }
}

export default sample;